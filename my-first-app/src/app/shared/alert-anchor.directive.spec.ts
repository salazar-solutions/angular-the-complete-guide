import { AlertAnchorDirective } from './alert-anchor.directive';

describe('AlertAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new AlertAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
